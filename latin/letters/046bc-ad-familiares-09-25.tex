% PLACEHOLDER --- Ad Familiares 9.25
% fetch_latin.py fell through to thelatinlibrary.com (Perseus URL absent in
% works.yaml entry) and dumped the whole fam9.shtml book file (~53 KB).
% The works.yaml entry lacks a Perseus URL and a recipient (recipient: null),
% which suggests the entry is a placeholder with no usable upstream source.
% Latin Library *does* show 9.25 as a discrete letter in its TOC, so the
% Perseus URL should be set correctly in works.yaml so a future fetch can
% extract the letter cleanly. Skipped in Cowork session 18.
